FOP: chia bài toán thành các hàm 
   : chúa dữ liệu chung , commom data
   : không có access modifi




A. lý thuyết
chương trình hướng đối tượng - oop
--> chia bài toán thành nhiều đối tượng 
--> Đối tượng: thuộc tính và phương thức 



B.Thực Hành
? làm sao để tạo ra đối tượng, đối tượng là gì?

Table --> class (ID,ChatLieu,KichThuoc)

? Vì sao các thuộc tính trong class theo OOP nên cho private rooif sử dụng setter, getter
để set/get thông itn mad không dùng public để code ngắn gọn
+ Nếu để public -->cac thuộc tính được phép truy cập từ mọi nơi giống như dât của FOP
không đúng với idea của OOP(quản lý dữ liệu cho tưfng đối tượng)
-->FOP: public all
-->OOP: private --> quản lí riêng cho từng object
      : nếu lỗi thì lỗi cho từng đối tượng ko ảnh hượng các đối tượng hàm của class khác
+ public cho getter setter khác với public cho từng thuộc tính
--> public attribute : bên ngoài gọi đến trực tiếp thuộc tính biết được 
   + cấu trúc dữ liệu của thuộc tính
   + giá trị của thuộc tính
--> public setter/gettter: bên ngoài truy cập thông qua hàm
   + tùy chức năng mà mình cho phép bên ngoài toàn bôh 1 phần cập nhật dưx liệu của thuộc tíh
--> truy cập get/set 1 phaafn , toàn bộ dữ liệu của thuộc tính mà hoàn toàn ko biết được gia trị thực 
tế của thuộc tính 
VD: NhanVien nhận lương ở công ty
2* --> truy cập get/set 1 phần , tòan bộ dữ liệu của thuộc tính mà hoàn toàn ko biết được giá trị
thực tế của thuộc tính là bao nhiêu
3*--> ko đc phép truy cập cấu trúc của dữ liệu








         