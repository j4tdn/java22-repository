package abstraction;

public class Ex01AbstractionTheoryDemo {
	/*
	 * Tính chất trừu tượng được thể hiện qua 
	 * 	1. class --> là 1 khái niệm trừu tượng dùng để chỉ tập hơp các object
	 * 				có chung thuộc tính , phương thức
	 * 	2. interface,abstract class --> chứa các abstract method sau đó được override lại trong các lớp thực thi
	 */
	
	
	//Functional interface
	//strategy expression
	//strategy pattern
	//design pattern(Mẫu thiết kế): MVC, MVVN, Builder
	
	//enum
	
	//boxing, auto-boxing
	
	//code convention = quy tắc = tiêu chuẩn code
	
	//1. Tên dự án
	// +Sử dụng tiếng anh
	// +Toàn bộ chữ cái viết thường
	// +Lesson-Tên bài học-
	// +Nếu có nhiều từ, cách nhau bởi -
	
	//2. Tên package
	// +sử dụng tiếng anh
	// +toàn bộ chữ cái viết thường
	// +nếu có nhiều từ 
			//.cách nhau bởi dấu _ VD:package_a
			//.cách nhau bởi dấu . VD:view.interface
	
	//3. Tên class/interface/abstract class
	// +sử dụng tiếng anh
	// +đặt tên theo quy tắc lạc đà -camel case convention
	//   . chữ cái đầu tiên viết hoa
	//   . chữ cái tiếp theo của mỗi từ viết hoa(nhiều từ) VD:IntCheck
	
	//4. 	tên hàm(function) - phương thức(method)
	// +sử dụng tiếng anh
	// + đặt tên theo quy tắc lạc đà 
	// +  chữ cái đầu tiên viết thường 
	// +  chữ cáu tiếp theo của mỗi từ viết hoa
	// +  phải là một động từ, cụm động từ (có ý  nghĩa)
	
	//5.  tên biến, thuộc tính
	// +sử dụng tiếng anh
		// + đặt tên theo quy tắc lạc đà 
		// +  chữ cái đầu tiên viết thường 
		//--> đặc biệt: biến là hằng số --> thường viết hoa 100%, các từ cách nhau bởi 	_
		// +  chữ cáu tiếp theo của mỗi từ viết hoa
		// +  phải là một danh từ, động từ, cụm động từ (có ý  nghĩa)
}
