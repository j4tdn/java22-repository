List<E>     --> Stream<E>
Set<E>      --> Stream<E>
Map<K,V>    --> Set<Entry<K,V>> --> Stream<Entry<K,V>>

use stream methods --> filter, map, sorted

collect((Collectors.toList())
collect((Collectors.toSet())
collect((Collectors.toMap(.....))

Đặc điểm :
 + Builder Pattern : sau khi từ source chuyển sang Stream<E>, gọi các hàm filter, map
 trả về stream, những hàm khi gọi vẫn trả về stream --> intermediate operations
 	+Strategy Pattern : 
 	--> khi gọi các intermediate operation phải truyền vào behavior mong muốn
 	
 	+ Khi gọi các hàm trả về không phải stream --> terminal operations
 	
 Câu hỏi: vì sao sau khi gọi các hàm của stream để xử lý xong mình không lưu lại kết quả trong 
 Stream mà phải chuyển đổi sang KDL khác