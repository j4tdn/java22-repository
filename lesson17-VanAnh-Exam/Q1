1/Primary key for result table is: student_id

2/list name, gender and teacher name
-select
student.name as student_name,
student.gender,
class.teacher as teacher_name
from student
inner join
class on student.class_id=class.id;


2.2
select
class.teacher as teacher_name,
count(student.id) as total_students
from student
inner join 
class on student.class_id=class.id
where class.name ='12C'
group by class.teacher

2.3
